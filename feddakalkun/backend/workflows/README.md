# Workflow Drop Zone

Legg API-format workflow JSON-filer her.

Du kan organisere dem i undermapper, for eksempel:

- `backend/workflows/z-image/`
- `backend/workflows/wan21/`
- `backend/workflows/wan22/`
- `backend/workflows/ltx/`

Backend-en eksponerer disse via `GET /api/workflow/list`.
